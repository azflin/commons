diff --git a/packages/opencode/src/cli/cmd/tui/routes/session/index.tsx b/packages/opencode/src/cli/cmd/tui/routes/session/index.tsx
index 70fcd421c..95e0cb201 100644
--- a/packages/opencode/src/cli/cmd/tui/routes/session/index.tsx
+++ b/packages/opencode/src/cli/cmd/tui/routes/session/index.tsx
@@ -7,6 +7,7 @@ import {
   For,
   Match,
   on,
+  onCleanup,
   onMount,
   Show,
   Switch,
@@ -1135,7 +1136,7 @@ export function Session() {
         }}
       >
         <box flexDirection="row" flexGrow={1} minHeight={0}>
-          <box flexGrow={1} minHeight={0} paddingBottom={1} paddingLeft={2} paddingRight={2} gap={1}>
+          <box flexGrow={1} minHeight={0} paddingBottom={tuiConfig.commons_theme_experimental ? 0 : 1} paddingTop={1} paddingLeft={2} paddingRight={2} gap={tuiConfig.commons_theme_experimental ? 0 : 1} backgroundColor={tuiConfig.commons_theme_experimental ? RGBA.fromInts(0, 0, 0, 160) : undefined}>
             <Show when={session()}>
               <scrollbox
                 ref={(r) => (scroll = r)}
@@ -1358,9 +1359,6 @@ function UserMessage(props: {
       <Show when={text()}>
         <box
           id={props.message.id}
-          border={["left"]}
-          borderColor={color()}
-          customBorderChars={SplitBorder.customBorderChars}
           marginTop={props.index === 0 ? 0 : 1}
         >
           <box
@@ -1372,12 +1370,12 @@ function UserMessage(props: {
             }}
             onMouseUp={props.onMouseUp}
             paddingTop={1}
-            paddingBottom={1}
+            paddingBottom={ctx.tui.commons_theme_experimental ? 0 : 1}
             paddingLeft={2}
             backgroundColor={hover() ? theme.backgroundElement : theme.backgroundPanel}
             flexShrink={0}
           >
-            <text fg={theme.text}>{text()}</text>
+            <text fg={theme.text}><span style={{ fg: color() }}>Γ¥▒</span> {text()}</text>
             <Show when={files().length}>
               <box flexDirection="row" paddingBottom={metadataVisible() ? 1 : 0} paddingTop={1} gap={1} flexWrap="wrap">
                 <For each={files()}>
@@ -1451,6 +1449,43 @@ function AssistantMessage(props: { message: AssistantMessage; parts: Part[]; las
 
   const childShortcut = useCommandShortcut("session.child.first")
 
+  const thinkingInfo = createMemo(() => {
+    if (!ctx.tui.commons_theme_experimental) return null
+    const part = props.parts.find((x) => x.type === "reasoning")
+    if (!part || part.type !== "reasoning") return null
+    const text = part.text.replace("[REDACTED]", "").trim()
+    if (!text) return null
+    const s = reasoningSummary(text)
+    const done = part.time.end !== undefined
+    return { title: s.title, done }
+  })
+
+  const baseColor = createMemo(() =>
+    props.message.error?.name === "MessageAbortedError"
+      ? theme.textMuted
+      : local.agent.color(props.message.agent),
+  )
+  const [cColor, setCColor] = createSignal(baseColor())
+  createEffect(() => {
+    const status = sync.session.status(props.message.sessionID)
+    if (status !== "working") {
+      setCColor(baseColor())
+      return
+    }
+    const clamp = (v: number) => Math.min(1, Math.max(0, v))
+    const blues = [
+      baseColor(),
+      RGBA.fromValues(clamp(baseColor().r * 0.7), clamp(baseColor().g * 0.7), clamp(baseColor().b * 1.3), 1),
+      RGBA.fromValues(clamp(baseColor().r * 1.2), clamp(baseColor().g * 1.2), clamp(baseColor().b * 0.8), 1),
+    ]
+    let i = 0
+    const timer = setInterval(() => {
+      i = (i + 1) % blues.length
+      setCColor(blues[i])
+    }, 800)
+    onCleanup(() => clearInterval(timer))
+  })
+
   return (
     <>
       <For each={props.parts}>
@@ -1496,15 +1531,12 @@ function AssistantMessage(props: { message: AssistantMessage; parts: Part[]; las
             <text marginTop={1}>
               <span
                 style={{
-                  fg:
-                    props.message.error?.name === "MessageAbortedError"
-                      ? theme.textMuted
-                      : local.agent.color(props.message.agent),
+                  fg: cColor(),
                 }}
               >
-                Γûú{" "}
+                ΓôÆ{" "}
               </span>{" "}
-              <span style={{ fg: theme.text }}>{Locale.titlecase(props.message.mode)}</span>
+              <span style={{ fg: theme.textMuted }}>{Locale.titlecase(props.message.mode)}</span>
               <span style={{ fg: theme.textMuted }}> ┬╖ {model()}</span>
               <Show when={duration()}>
                 <span style={{ fg: theme.textMuted }}> ┬╖ {Locale.duration(duration())}</span>
@@ -1512,6 +1544,9 @@ function AssistantMessage(props: { message: AssistantMessage; parts: Part[]; las
               <Show when={props.message.error?.name === "MessageAbortedError"}>
                 <span style={{ fg: theme.textMuted }}> ┬╖ interrupted</span>
               </Show>
+              <Show when={thinkingInfo()}>
+                <span style={{ fg: theme.textMuted }}> ┬╖ Thought{thinkingInfo()!.title ? `: ${thinkingInfo()!.title}` : ""}</span>
+              </Show>
             </text>
           </box>
         </Match>
@@ -1557,16 +1592,18 @@ function ReasoningPart(props: { last: boolean; part: ReasoningPart; message: Ass
 
   return (
     <Show when={content()}>
-      <box id={"text-" + props.part.id} paddingLeft={3} marginTop={1} flexDirection="column" flexShrink={0}>
-        <box onMouseUp={toggle}>
-          <ReasoningHeader
-            toggleable={inMinimal()}
-            open={!inMinimal() || expanded()}
-            done={isDone()}
-            title={summary().title}
-            duration={isDone() ? Locale.duration(duration()) : undefined}
-          />
-        </box>
+      <box id={"text-" + props.part.id} paddingLeft={3} marginTop={ctx.tui.commons_theme_experimental ? 0 : 1} flexDirection="column" flexShrink={0}>
+        <Show when={!ctx.tui.commons_theme_experimental}>
+          <box onMouseUp={toggle}>
+            <ReasoningHeader
+              toggleable={inMinimal()}
+              open={!inMinimal() || expanded()}
+              done={isDone()}
+              title={summary().title}
+              duration={isDone() ? Locale.duration(duration()) : undefined}
+            />
+          </box>
+        </Show>
         <Show when={(!inMinimal() || expanded()) && summary().body}>
           <box paddingLeft={inMinimal() ? 2 : 0} marginTop={1}>
             <code
